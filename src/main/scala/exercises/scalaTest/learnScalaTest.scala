package exercises.scalaTest

object LearnScalaTestClass {
  def addiction(firstNumber: Int, secondNumber: Int): Int = {
    firstNumber + secondNumber
  }

  def returnANumber(str: String): Int = {
    str.toInt
  }
  val list = List(1, 2, 3, 4)
}
