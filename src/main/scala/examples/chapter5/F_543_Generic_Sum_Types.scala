package examples.chapter5

class F_543_Generic_Sum_Types {
  /*
   using generics to model a sum type

  explicitly represent the disjunction

  def intOrString(input: Boolean): Sum[Int, String] =
      if(input == true) {
        Left[Int, String](123)
      } else {
        Right[Int, String]("abc")
      }
    // intOrString: (input: Boolean)sum.Sum[Int,String]
 */
}
